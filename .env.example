# Copy to .env and fill in if you have real credentials. The app runs in
# mock mode automatically when these are absent — no keys required to run
# the server or the test suite.

RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
ANTHROPIC_API_KEY=

SENTINEL_DB_PATH=./data/sentinel.db
SENTINEL_KEY_DIR=./data/keys
